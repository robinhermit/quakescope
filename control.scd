~aa.(); // hard reset! use only if you have troubles

~bb.(); // send SynthDefs, launch GUI

~cc.(); // after having readings from arduino, start eventStream

~dd.(); // start binauralRev after eventStream

~ee.(); // start action as last